{
  "name": "Phoenix-XShare",
  "version": "1.0.0",
  "type": "module",
  "description": "Premium secure file sharing portal written in Next.js & Express",
  "main": "./backend/src/index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && PORT=5000 node src/index.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "concurrently \"cd backend && PORT=5000 node src/index.js\" \"cd frontend && npm start\""
  },
  "keywords": [],
  "author": "Pheonix",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
